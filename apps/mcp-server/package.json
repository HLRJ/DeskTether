{
  "name": "@desktether/mcp-server",
  "version": "0.2.4",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "desktether": "dist/index.js"
  },
  "scripts": {
    "test": "pnpm --filter @desktether/core build && vitest run",
    "typecheck": "pnpm --filter @desktether/core build && tsc --noEmit",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "@desktether/core": "workspace:*",
    "@modelcontextprotocol/server": "^2.0.0",
    "zod": "^4.6.5"
  }
}
